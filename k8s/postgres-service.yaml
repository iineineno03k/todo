apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: postgres
spec:
  type: ClusterIP  # Internal communication only
  selector:
    app: postgres
  ports:
  - protocol: TCP
    port: 5432        # Service port (used by backend)
    targetPort: 5432  # Container port (postgres pod)
